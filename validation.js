// attach keyup events to inputs
inputs.forEach(input => {
  input.addEventListener('keyup', e => {
    //getting input value
    //test with your regex
    // if(e.target.name === 'email'){
    //   e.target.value.test(//)
    // }
  })
})
